{
  "frontend/src/App.tsx": "Main app entry point with TanStack Router; wraps the layout in an `AppWithTheme` component that calls `useThemeLoader` so theme colors are applied on every page load before rendering routes.",
  "frontend/src/components/admin/AdminGalleryTab.tsx": "Admin tab for managing the before/after gallery — allows uploading image pairs with description, service, and location metadata using ExternalBlob, and displays/deletes existing gallery entries.",
  "frontend/src/components/admin/AdminBookingsTab.tsx": "Admin tab that displays all submitted booking requests in expandable cards showing customer name, service type, location, date, email, phone, and message, with quick reply-by-email and WhatsApp action buttons.",
  "frontend/src/hooks/useAdminCheck.ts": "Compatibility hook that wraps useAdminSession to provide isAdmin, isLoading, and isAuthenticated states based on the username/password admin session (no longer depends on Internet Identity or useIsCallerAdmin).",
  "frontend/src/components/admin/AdminTranslationsTab.tsx": "Admin tab for editing Greek and Russian translations, displaying all translation keys grouped by section with English as read-only reference, editable fields for each language, reset-to-default per key, and save-to-localStorage functionality that immediately updates the public site.",
  "frontend/src/components/ServicesSection.tsx": "Services grid section with all text updated to explicit `text-white` for headings and `text-white/75` for body/description text, replacing `text-foreground`/`text-muted-foreground` for guaranteed contrast on dark backgrounds.",
  "frontend/src/components/admin/AdminServicesTab.tsx": "Admin tab for managing services with add/edit/delete functionality and image upload support. Adds the same `getImageUrl` safety helper to guard all `getDirectURL` calls, and also shows existing service images in the list view and edit form.",
  "frontend/src/hooks/useQueries.ts": "All React Query hooks for backend operations, now including `useGetSiteTheme` (query) and `useSetSiteTheme` (mutation) for fetching and saving the site theme via `actor.getSiteTheme()` and `actor.setSiteTheme()`.",
  "frontend/src/hooks/useThemeLoader.ts": "Custom hook that fetches the site theme from the backend via `useGetSiteTheme` and injects `--theme-primary`, `--theme-accent`, and `--theme-bg` as CSS custom properties on `:root`, with fallback to the default black and orange palette.",
  "frontend/src/components/admin/AdminThemeTab.tsx": "New admin tab component with color picker inputs for `primaryColor`, `accentColor`, and `backgroundColor`; pre-fills from backend, shows a live preview strip, and saves via `useSetSiteTheme` with success/error feedback.",
  "frontend/src/pages/AdminPanel.tsx": "Admin panel with tabbed navigation; adds a new \"Theme\" tab that renders `AdminThemeTab` alongside the existing Homepage, Services, Testimonials, Gallery, Bookings, and Translations tabs.",
  "frontend/src/components/Layout.tsx": "Site layout with header, nav, and footer; header now always uses `bg-steel-900` (no transparent/white state on scroll) and all nav link text is explicitly `text-white` to guarantee legibility at all times.",
  "frontend/src/components/ContactSection.tsx": "Contact section with all text updated to explicit `text-white` for headings and contact values, `text-white/55` for labels, `text-white/60` for social media section header, and `text-white/70` for the footer caption text.",
  "frontend/src/components/AboutSection.tsx": "About section with all text updated to explicit `text-white` for headings and highlight labels, and `text-white/80` for body paragraphs, replacing `text-foreground`/`text-muted-foreground` for maximum contrast.",
  "frontend/src/components/HeroSection.tsx": "Hero section with all text updated to explicit `text-white` for the main title, `text-white/85` for the subtitle, and `text-white/70` for stat labels, ensuring full visibility against the dark overlay.",
  "frontend/src/utils/translations.ts": "Full EN/GR/RU translation definitions for all site copy, with all location references updated to Cyprus. Exports `translations`, `defaultTranslations` (alias of `translations` for backward compatibility with `useTranslation` and `AdminTranslationsTab`), and the `getTranslation` helper function.",
  "frontend/src/index.css": "Global styles with OKLCH design tokens; bumps `--foreground` and `--card-foreground` to pure white (`oklch(1.0 0 0)`) and `--muted-foreground` to a much lighter value (`oklch(0.85 0.01 240)`) and `--nav-text` to pure white for maximum contrast on dark backgrounds.",
  "frontend/src/components/TestimonialsSection.tsx": "Testimonials section with all text updated to explicit `text-white` for headings and customer names, `text-white/80` for testimonial body text, and `text-white/60` for service/location metadata.",
  "frontend/src/components/GallerySection.tsx": "Gallery section with all text updated to explicit `text-white` for headings and item titles, `text-white/65` for metadata, and `text-white/70` for inactive toggle button text.",
  "frontend/src/components/BookingForm.tsx": "Booking form section with all translation key references corrected to use valid `TranslationKey` values — `booking.success`, `booking.successMsg`, `booking.service`, `booking.selectService`, and `booking.date` — with hardcoded fallback strings for keys that don't exist in the translation system."
}